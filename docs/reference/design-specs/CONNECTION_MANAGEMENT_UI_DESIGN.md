# Connection 管理 UI 設計規格（Phase 1.2 / 1.3）

> **上位架構**：請先參考 `ADMIN_APP_UI_ARCHITECTURE.md`，本文件為其中「Connections」模組的具體規格。

**最後更新**: 2025-11-11  
**適用範圍**: 管理後台 `Connection` 與 `Connection Item` 操作體驗（Shopline MVP，預留多平台擴充）

---

## 🎯 設計目標

1. **統一 Connection 體驗**：以「平台 × 帳戶」為核心，建立清楚的瀏覽、切換、狀態辨識與操作流程。  
2. **可擴充的 UI 架構**：UI 分層明確，支援後續新增平台、能力標記（Capability Flags）與更多操作（停用、筆記、Webhook 設定等）。  
3. **落實方法論原則**：遵循 `connection-state-sync`、`state-management` 決策，Zustand 為單一 Source of Truth，URL 僅初始化。  
4. **降低重複調整**：提供交互流程藍圖與組件清單，避免 UI 每次新增功能就大幅重寫。  
5. **易於測試與延伸**：配套測試用例、狀態矩陣，確保 Run 中可快速驗證。

---

## 🧱 資訊架構

### 1. 頂層結構

```
┌──────────────────────────────┐
│ Global Header (既有)         │
├───────────────┬──────────────┤
│ Connection Rail│ Workspace   │
│ (平台/帳戶選單)│ Canvas       │
├───────────────┴──────────────┤
│ Activity / Insight Panel     │
└──────────────────────────────┘
```

- **Connection Rail**：置左垂直選單，顯示平台分組與 Connection，允許篩選、搜尋與建立新 Connection。  
- **Workspace Canvas**：依分頁（Tab）呈現列表、詳細面板與操作模組。Phase 1.2 先提供 `Overview`、`Connection Items`、`Events` 三個基礎分頁。  
- **Activity / Insight Panel**：底部可折疊面板（Phase 2 起啟用），顯示最近授權、錯誤、Webhook 活動，Phase 1.2 保留容器但顯示空態。

### 2. Connection Rail 詳細規格

| 區塊 | 說明 | 備註 |
|------|------|------|
| 平台 Segmented Control | 列出目前支援平台（Phase 1.2 先顯示 Shopline，Phase 1.3 加入 Next Engine） | 使用平台圖示 + 名稱；支援 `All` 檢視 |
| Connection List | 每筆顯示 `displayName`、狀態徽章、到期時間（若有） | 狀態：Active、Expired、Error，與 `Token Lifecycle` 決策對應 |
| 快速操作 | Hover 顯示「重新授權」、「更多」(dropdown) | Phase 1.2 開啟重新授權；停用/刪除保留在詳情頁 |
| 建立按鈕 | `+ 新增 Connection` | 呼叫 Story 4.2 Flow，進入平台選擇與 OAuth |

### 3. Workspace Canvas

#### 3.1 Overview Tab

- **目標**：摘要 Connection 狀態、授權資訊、錯誤提醒。  
- **模組**：
  - `ConnectionSummaryCard`：顯示平台徽章、授權資訊與統計。欄位如下：
    - 平台資訊：平台徽章 + 平台名稱（`platform`），平台帳戶識別（Shopline 顯示 `handle`）。
    - 授權狀態：Active / Expired / Error，對應 Token Lifecycle 決策，附狀態徽章。
    - Token 資訊：到期時間（`tokenExpiresAt`），最近重新授權時間（若有）。
    - 建立 / 更新：Connection 建立時間、最後更新時間。
    - Owner 資訊：建立連線的使用者（Owner email / name）。
    - 數量統計（預留）：Connection Items 數量、Webhook 綁定數（Phase 1.2 可顯示 Items 數量，其他指標預留位置）。
  - `StatusTimeline`（Phase 1.2 可顯示兩筆事件，Phase 2 擴充為時間軸）。  
  - `QuickActionsGrid`：包含重新授權、同步、查看 Platform Docs（連結 `platform-apis/`）。
    - **重新授權**：針對該 Connection (平台帳戶) 重新觸發 OAuth 授權。完成後需刷新整個 Connection 的 access token 與對應 Connection Items。
    - **刷新 Token**（若平台提供 refresh endpoint）：顯示「Refresh Token」按鈕，呼叫後端 refresh API，成功後更新 token 到期時間並寫入 Activity Dock。若平台不支援 refresh（如 Shopline 直接透過 OAuth 重授權），按鈕可隱藏。
  - `ConnectionItemsPreview`：顯示該 Connection 底下前 N 筆 Connection Item 卡片。每筆包含：
    - Item 名稱（`displayName`）
    - 外部資源識別（Shopline 顯示店鋪 `externalResourceId / handle`）
    - 狀態徽章（Active / Disabled）
    - 最近同步時間（若尚未同步顯示空態）
    - 按鈕「查看全部」導向 Connection Items Tab。
    - **簡潔策略**：若 Connection 底下只有 0~1 筆 Item，預設將 Preview 收合為簡短摘要，直接在 `ConnectionSummaryCard` 內顯示「主要商店：{handle}」即可；只有在多筆 Item 時才展開卡片列表，避免 UI 過度分層。

#### 3.2 Connection Items Tab

- **目標**：呈現 Connection 底下的商店/資源，支援分頁、搜尋、狀態切換。  
- **表格欄位**：`名稱`、`資源 ID`、`狀態`、`最近同步`、`操作`。  
- **操作**：  
  - `查看詳情`：開啟側欄，顯示 Metadata（domain、scope 等）。  
  - `停用/啟用`：調用後端 API（Story 4.3）確保權限檢查。  
  - Phase 1.3 加入多平台欄位，例如 Next Engine 的 `companyId`。

#### 3.3 Events Tab

- **目標**：整合與該 Connection 相關的 Webhook、錯誤事件。  
- **元件**：  
  - `EventFilterBar`：依狀態、事件類型、時間範圍篩選。  
  - `EventList`：引用既有 `WebhookEventCard`，新增 Connection 篩選。

---

## 🔁 使用流程（User Flow）

### Flow C1：查看並切換 Connection
1. 使用者進入 Connection 頁面，Zustand 由 URL 初始化（若無參數顯示空態）。
2. Connection Rail 顯示所有 Connection；點擊後更新 Zustand，Workspace 即時切換內容。
3. URL 不自動回寫，保證單一來源。

### Flow C2：新增 Connection
1. 點擊 `+ 新增 Connection`，開啟平台選擇器（Phase 1.2 只有 Shopline）。
2. 顯示 OAuth 引導 Modal（含操作步驟、測試指引連結）。
3. 完成 OAuth 後，後端 callback 觸發前端 `ConnectionCreateSuccess` toast，並選取新 Connection。
4. 回到 Overview Tab，自動顯示新 Connection 資訊。

### Flow C3：重新授權
1. 在 Connection Rail 或 Overview Tab 點擊「重新授權」。
2. 開啟 Modal 說明失效原因（對應後端錯誤碼）。
3. 使用者進行 OAuth 流程；完成後回到 Overview Tab，自動刷新狀態。

### Flow C4：停用/啟用 Connection Item
1. 在 Connection Items Tab 選擇資源，點擊「停用」。
2. 彈出確認對話框，顯示影響（停用後 Webhook 停用、API 需重新授權等）。
3. 呼叫後端 API，成功後狀態更新，記錄於 Activity Panel。

---

## 🧩 UI 元件與樣式

| 元件 | 說明 | 實作位置 |
|------|------|---------|
| `ConnectionPlatformBadge` | 平台徽章，顯示平台名稱與顏色 | `components/connections/ConnectionPlatformBadge.tsx` |
| `ConnectionStatusPill` | 狀態標籤（Active、Expired、Error） | 共用於列表與 Summary Card |
| `ConnectionRail` | 左側導覽，封裝平台過濾、搜尋、列表 | 新增於 `components/connections` |
| `ConnectionTabs` | Workspace Tab 控制 | 依賴 Zustand state |
| `ConnectionDetailPanel` | 側欄顯示 Connection Item 詳細資訊 | Story 4.3 引入 |
| `ConnectionActivityTimeline` | Activity Panel 內容（Phase 2） | 可返回空態 | 

> 樣式統一使用 Tailwind CSS，命名遵循 BEM-like Utility 組合，並集中於 `components/connections/*.styles.ts`（若需抽離）。

---

## 🧪 測試與驗收要點

### 1. UI 狀態矩陣

| 平台 | Connection 狀態 | 預期顯示 | 验證點 |
|------|----------------|----------|--------|
| Shopline | Active | 綠色徽章、顯示到期時間 | 切換時資料不閃爍 |
| Shopline | Expired | 黃色徽章、顯示重新授權入口 | 重新授權後狀態更新 |
| Shopline | Error | 紅色徽章、顯示錯誤訊息摘要 | 點擊查看詳細錯誤 |
| Next Engine (Phase 1.3) | Active | 平台徽章顯示 `NE` | 資料欄位顯示 companyId |

### 2. 功能測試清單
- Zustand 初始化僅執行一次；切換 Connection 不觸發 route loop。  
- 新增 Connection 後，列表自動插入並選中。  
- 重新授權流程：
  - 觸發 OAuth → 返回後狀態刷新  
  - Toast 顯示成功訊息並提供查看詳情連結  
- Connection Item 停用/啟用：狀態與操作按鈕同步更新，並記錄於 Activity Panel。  
- Events Tab 篩選：以 Connection ID + 平台做查詢，確保資料隔離。  
- 無權限或 session 過期時，顯示統一錯誤提示（參考 `Token Lifecycle` 決策）。

### 3. User Test 建議
- 以 Admin 帳號登入 → 檢視多個 Shopline Connection → 切換無閃跳。  
- 新增 Connection → 完成 OAuth → 回到頁面確認即時顯示。  
- 模擬 Token 過期 → 點擊重新授權 → 驗證流程與提示。  
- 停用某 Connection Item → 從另一個 Connection 切換再回來 → 確認狀態保留。  
- Phase 1.3：新增 Next Engine 連線 → 驗證平台切換、欄位差異。 

---

## 🔭 擴充考量

1. **平台 Capability**：在資料層加上 `capabilities` 標記（例如：`supportsWebhook`, `supportsBulkAPI`），UI 根據能力顯示或隱藏模組。  
2. **多 Connection 角色管理**：預留 `Owner`、`Collaborator` 欄位，未來提供共享管理。  
3. **多語系 / 國際化**：UI 字串集中於 `locales/`，Phase 2 起納入 i18n。  
4. **動態路由（Refactor R3.3）**：待動態路由上線後，Connection Detail 可透過 `/connections/[platform]/[connectionId]` 分享。  
5. **Insight / 通知中心**：Activity Panel 改為即時更新（WebSocket/SSE），提供告警與操作建議。  
6. **行動裝置支援**：現階段以桌機為主，預留 responsive breakpoint；Phase 2.2 評估多裝置登入需求。

---

## 📚 相關文件
- `docs/backlog/epics/epic-4-multi-store-management.md` – Epic 4 核心規劃  
- `docs/memory/decisions/connection-state-sync.md` – URL ↔︎ Zustand 策略  
- `docs/memory/decisions/token-lifecycle-handling.md` – 錯誤碼與重新授權  
- `docs/memory/decisions/state-management.md` – Zustand 標準實踐  
- `docs/reference/design-specs/WEBHOOK_TEST_UI_DESIGN.md` – Webhook UI 參考  
- `docs/reference/design-specs/ADMIN_API_TEST_UI_DESIGN.md` – Admin 測試介面規格

